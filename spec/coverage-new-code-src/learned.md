# Learned

## Verified
- Coverage target: >=95% for changed code under src, with small exceptions if infeasible (per user request).

## UNVERIFIED
- Exact list of changed files under src for this work (to be verified via git diff).
- Which test projects map to each changed src file (to be verified via project references).
