using System.Collections.Generic;
using System.Text;

using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.Diagnostics;
using Microsoft.CodeAnalysis.Text;

using Mississippi.Inlet.Generators.Core.Emit;


namespace Mississippi.Inlet.Client.Generators;

/// <summary>
///     Generates client-side action effect classes for sagas marked with [GenerateSagaEndpoints].
/// </summary>
[Generator(LanguageNames.CSharp)]
public sealed class SagaClientActionEffectsGenerator : IIncrementalGenerator
{
    private static void GenerateActionEffect(
        SourceProductionContext context,
        SagaClientGeneratorHelper.SagaClientInfo saga
    )
    {
        string actionName = $"Start{saga.SagaName}SagaAction";
        string effectTypeName = $"Start{saga.SagaName}SagaActionEffect";
        string dtoName = $"Start{saga.SagaName}SagaRequestDto";
        string stateTypeName = $"{saga.SagaName}SagaState";
        SourceBuilder sb = new();
        sb.AppendAutoGeneratedHeader();
        sb.AppendLine("using System;");
        sb.AppendLine("using System.Net.Http;");
        sb.AppendLine();
        sb.AppendUsing("Mississippi.Common.Abstractions.Mapping");
        sb.AppendUsing("Mississippi.Inlet.Client.Abstractions.ActionEffects");
        sb.AppendLine();
        sb.AppendLine($"using {saga.ActionsNamespace};");
        sb.AppendLine($"using {saga.DtosNamespace};");
        sb.AppendLine($"using {saga.StateNamespace};");
        sb.AppendFileScopedNamespace(saga.ActionEffectsNamespace);
        sb.AppendLine();
        sb.AppendSummary($"Action effect that handles the {saga.SagaName} saga start request.");
        sb.AppendLine("/// <remarks>");
        sb.AppendLine("///     <para>");
        sb.AppendLine(
            $"///         This action effect posts to the {saga.SagaName} saga endpoint at <c>/api/sagas/{saga.RoutePrefix}/{{sagaId}}</c>.");
        sb.AppendLine("///     </para>");
        sb.AppendLine("/// </remarks>");
        sb.AppendGeneratedCodeAttribute("SagaClientActionEffectsGenerator");
        sb.AppendLine($"internal sealed class {effectTypeName}");
        sb.IncreaseIndent();
        sb.AppendLine($": CommandActionEffectBase<{actionName}, {dtoName}, {stateTypeName},");
        sb.AppendLine(
            $"    Start{saga.SagaName}SagaExecutingAction, Start{saga.SagaName}SagaSucceededAction, Start{saga.SagaName}SagaFailedAction>");
        sb.DecreaseIndent();
        sb.OpenBrace();
        sb.AppendLine("/// <summary>");
        sb.AppendLine($"///     Initializes a new instance of the <see cref=\"{effectTypeName}\" /> class.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("/// <param name=\"httpClient\">The HTTP client for API calls.</param>");
        sb.AppendLine("/// <param name=\"mapper\">The mapper for action-to-DTO conversion.</param>");
        sb.AppendLine("/// <param name=\"timeProvider\">The time provider for timestamps.</param>");
        sb.AppendLine($"public {effectTypeName}(");
        sb.IncreaseIndent();
        sb.AppendLine("HttpClient httpClient,");
        sb.AppendLine($"IMapper<{actionName}, {dtoName}> mapper,");
        sb.AppendLine("TimeProvider? timeProvider = null");
        sb.DecreaseIndent();
        sb.AppendLine(")");
        sb.IncreaseIndent();
        sb.AppendLine(": base(httpClient, mapper, timeProvider)");
        sb.DecreaseIndent();
        sb.OpenBrace();
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine("/// <inheritdoc />");
        sb.AppendLine($"protected override string AggregateRoutePrefix => \"/api/sagas/{saga.RoutePrefix}\";");
        sb.AppendLine();
        sb.AppendLine("/// <inheritdoc />");
        sb.AppendLine("protected override string Route => string.Empty;");
        sb.AppendLine();
        sb.AppendLine("/// <inheritdoc />");
        sb.AppendLine($"protected override string GetEndpoint({actionName} action) =>");
        sb.IncreaseIndent();
        sb.AppendLine("$\"{AggregateRoutePrefix}/{action.SagaId}\";");
        sb.DecreaseIndent();
        sb.CloseBrace();
        context.AddSource($"{effectTypeName}.g.cs", SourceText.From(sb.ToString(), Encoding.UTF8));
    }

    private static void GenerateResumeActionEffect(
        SourceProductionContext context,
        SagaClientGeneratorHelper.SagaClientInfo saga
    )
    {
        string actionName = $"Resume{saga.SagaName}SagaAction";
        string effectTypeName = $"Resume{saga.SagaName}SagaActionEffect";
        string stateTypeName = $"{saga.SagaName}SagaState";
        SourceBuilder sb = new();
        sb.AppendAutoGeneratedHeader();
        sb.AppendLine("using System;");
        sb.AppendLine("using System.Collections.Generic;");
        sb.AppendLine("using System.Net.Http;");
        sb.AppendLine("using System.Net.Http.Json;");
        sb.AppendLine("using System.Runtime.CompilerServices;");
        sb.AppendLine("using System.Threading;");
        sb.AppendLine("using System.Threading.Tasks;");
        sb.AppendLine();
        sb.AppendUsing("Mississippi.DomainModeling.Abstractions");
        sb.AppendUsing("Mississippi.Inlet.Client.Abstractions.Actions");
        sb.AppendUsing("Mississippi.Reservoir.Abstractions");
        sb.AppendUsing("Mississippi.Reservoir.Abstractions.Actions");
        sb.AppendLine();
        sb.AppendLine($"using {saga.ActionsNamespace};");
        sb.AppendLine($"using {saga.StateNamespace};");
        sb.AppendFileScopedNamespace(saga.ActionEffectsNamespace);
        sb.AppendLine();
        sb.AppendSummary($"Action effect that handles manual resume requests for the {saga.SagaName} saga.");
        sb.AppendLine("/// <remarks>");
        sb.AppendLine("///     <para>");
        sb.AppendLine(
            $"///         This action effect posts to the {saga.SagaName} saga manual resume endpoint at <c>/api/sagas/{saga.RoutePrefix}/{{sagaId}}/resume</c> and parses <see cref=\"SagaResumeResponse\" />.");
        sb.AppendLine("///     </para>");
        sb.AppendLine("/// </remarks>");
        sb.AppendGeneratedCodeAttribute("SagaClientActionEffectsGenerator");
        sb.AppendLine($"internal sealed class {effectTypeName} : IActionEffect<{stateTypeName}>");
        sb.OpenBrace();
        sb.AppendLine("private HttpClient Http { get; }");
        sb.AppendLine("private TimeProvider TimeProvider { get; }");
        sb.AppendLine();
        sb.AppendLine("/// <summary>");
        sb.AppendLine($"///     Initializes a new instance of the <see cref=\"{effectTypeName}\" /> class.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("/// <param name=\"httpClient\">The HTTP client for API calls.</param>");
        sb.AppendLine("/// <param name=\"timeProvider\">The time provider for timestamps.</param>");
        sb.AppendLine($"public {effectTypeName}(");
        sb.IncreaseIndent();
        sb.AppendLine("HttpClient httpClient,");
        sb.AppendLine("TimeProvider? timeProvider = null");
        sb.DecreaseIndent();
        sb.AppendLine(")");
        sb.OpenBrace();
        sb.AppendLine("ArgumentNullException.ThrowIfNull(httpClient);");
        sb.AppendLine("Http = httpClient;");
        sb.AppendLine("TimeProvider = timeProvider ?? TimeProvider.System;");
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine("/// <inheritdoc />");
        sb.AppendLine("public bool CanHandle(");
        sb.IncreaseIndent();
        sb.AppendLine("IAction action");
        sb.DecreaseIndent();
        sb.AppendLine(") =>");
        sb.IncreaseIndent();
        sb.AppendLine($"action is {actionName};");
        sb.DecreaseIndent();
        sb.AppendLine();
        sb.AppendLine("/// <inheritdoc />");
        sb.AppendLine("public async IAsyncEnumerable<IAction> HandleAsync(");
        sb.IncreaseIndent();
        sb.AppendLine("IAction action,");
        sb.AppendLine($"{stateTypeName} currentState,");
        sb.AppendLine("[EnumeratorCancellation] CancellationToken cancellationToken");
        sb.DecreaseIndent();
        sb.AppendLine(")");
        sb.OpenBrace();
        sb.AppendLine("_ = currentState;");
        sb.AppendLine($"if (action is not {actionName} typedAction)");
        sb.OpenBrace();
        sb.AppendLine("yield break;");
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine("string commandId = Guid.NewGuid().ToString(\"N\");");
        sb.AppendLine($"yield return Resume{saga.SagaName}SagaExecutingAction.Create(");
        sb.IncreaseIndent();
        sb.AppendLine("commandId,");
        sb.AppendLine($"typeof({actionName}).Name,");
        sb.AppendLine("TimeProvider.GetUtcNow());");
        sb.DecreaseIndent();
        sb.AppendLine();
        sb.AppendLine("SagaResumeResponse? response = null;");
        sb.AppendLine("string? errorMessage = null;");
        sb.AppendLine("try");
        sb.OpenBrace();
        sb.AppendLine("using HttpResponseMessage httpResponse = await Http.PostAsync(");
        sb.IncreaseIndent();
        sb.AppendLine("GetEndpoint(typedAction),");
        sb.AppendLine("content: null,");
        sb.AppendLine("cancellationToken);");
        sb.DecreaseIndent();
        sb.AppendLine();
        sb.AppendLine("if (!httpResponse.IsSuccessStatusCode)");
        sb.OpenBrace();
        sb.AppendLine("string responseBody = await httpResponse.Content.ReadAsStringAsync(cancellationToken);");
        sb.AppendLine("errorMessage = $\"Server error ({(int)httpResponse.StatusCode}): {responseBody}\";");
        sb.CloseBrace();
        sb.AppendLine("else");
        sb.OpenBrace();
        sb.AppendLine("response = await httpResponse.Content.ReadFromJsonAsync<SagaResumeResponse>(cancellationToken);");
        sb.CloseBrace();
        sb.CloseBrace();
        sb.AppendLine("catch (HttpRequestException ex)");
        sb.OpenBrace();
        sb.AppendLine("errorMessage = $\"Network error: {ex.Message}\";");
        sb.CloseBrace();
        sb.AppendLine("catch (TaskCanceledException ex)");
        sb.OpenBrace();
        sb.AppendLine("errorMessage = $\"Request cancelled: {ex.Message}\";");
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine("if (errorMessage is not null)");
        sb.OpenBrace();
        sb.AppendLine($"yield return Resume{saga.SagaName}SagaFailedAction.Create(");
        sb.IncreaseIndent();
        sb.AppendLine("commandId,");
        sb.AppendLine("\"HttpError\",");
        sb.AppendLine("errorMessage,");
        sb.AppendLine("TimeProvider.GetUtcNow());");
        sb.DecreaseIndent();
        sb.AppendLine("yield break;");
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine("if (response is null)");
        sb.OpenBrace();
        sb.AppendLine($"yield return Resume{saga.SagaName}SagaFailedAction.Create(");
        sb.IncreaseIndent();
        sb.AppendLine("commandId,");
        sb.AppendLine("\"NoResponse\",");
        sb.AppendLine("\"No response from server.\",");
        sb.AppendLine("TimeProvider.GetUtcNow());");
        sb.DecreaseIndent();
        sb.AppendLine("yield break;");
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine($"yield return Resume{saga.SagaName}SagaSucceededAction.Create(");
        sb.IncreaseIndent();
        sb.AppendLine("commandId,");
        sb.AppendLine("response,");
        sb.AppendLine("TimeProvider.GetUtcNow());");
        sb.DecreaseIndent();
        sb.CloseBrace();
        sb.AppendLine();
        sb.AppendLine($"private static string GetEndpoint({actionName} action) =>");
        sb.IncreaseIndent();
        sb.AppendLine($"$\"/api/sagas/{saga.RoutePrefix}/{{action.SagaId}}/resume\";");
        sb.DecreaseIndent();
        sb.CloseBrace();
        context.AddSource($"{effectTypeName}.g.cs", SourceText.From(sb.ToString(), Encoding.UTF8));
    }

    /// <inheritdoc />
    public void Initialize(
        IncrementalGeneratorInitializationContext context
    )
    {
        IncrementalValueProvider<(Compilation Compilation, AnalyzerConfigOptionsProvider Options)>
            compilationAndOptions = context.CompilationProvider.Combine(context.AnalyzerConfigOptionsProvider);
        IncrementalValueProvider<List<SagaClientGeneratorHelper.SagaClientInfo>> sagasProvider =
            compilationAndOptions.Select((
                source,
                _
            ) => SagaClientGeneratorHelper.GetSagasFromCompilation(source.Compilation, source.Options));
        context.RegisterSourceOutput(
            sagasProvider,
            static (
                spc,
                sagas
            ) =>
            {
                foreach (SagaClientGeneratorHelper.SagaClientInfo saga in sagas)
                {
                    GenerateActionEffect(spc, saga);
                    GenerateResumeActionEffect(spc, saga);
                }
            });
    }
}