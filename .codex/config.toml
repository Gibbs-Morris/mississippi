# Worker defaults for the installed Codex CLI 0.154 configuration contract.

[agents]
enabled = true
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "max"
max_concurrent_threads_per_session = 8
