# Mississippi.AspNetCore.Orleans.L0Tests

Unit tests for the Mississippi.AspNetCore.Orleans project.

## Test Organization

Tests follow the L0 pattern for pure unit tests with minimal dependencies.

Test naming convention: `MethodName_Should_Outcome_GivenCondition`

## Running Tests

```bash
dotnet test
```
