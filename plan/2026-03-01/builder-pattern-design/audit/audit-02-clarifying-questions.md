# 02 Clarifying Questions

## (A) Answered from repo evidence

### AQ1: Do we already have a full host-builder contract family?
- Answer: No.
- Evidence:
  - No matches for `IClientBuilder`, `IGatewayBuilder`, `IRuntimeBuilder` in `src/**/*.cs`.
  - Existing entry points are `AddInletClient`, `AddInletServer`, `AddInletSilo` extension methods.
- Triangulation: search results + direct file inspection.

### AQ2: Is there an existing builder pattern we can mirror?
- Answer: Yes, but localized.
- Evidence:
  - `InletBlazorSignalRBuilder` fluent + `Build()` in `src/Inlet.Client/InletBlazorSignalRBuilder.cs`.
  - Consumed via `AddInletBlazorSignalR(... Action<InletBlazorSignalRBuilder>)` in `src/Inlet.Client/InletBlazorRegistrations.cs`.
- Triangulation: concrete builder + registration integration.

### AQ3: Is feature-state registration currently explicit and composable?
- Answer: Yes.
- Evidence:
  - `ReservoirRegistrations` has `AddFeatureState`, `AddReducer`, `AddActionEffect`, `AddReservoir`.
  - `IFeatureState` + `IFeatureStateRegistration` contracts in Reservoir abstractions.
- Triangulation: runtime registrations + abstraction contracts.

### AQ4: Are generated composition layers already doing host/domain composition?
- Answer: Yes.
- Evidence:
  - Client generator emits `DomainFeatureRegistrations`.
  - Runtime generator emits `DomainSiloRegistrations`; gateway generator emits `DomainServerRegistrations`.
- Triangulation: three independent generator modules.

### AQ5: Does docs IA already include a proposal/future-design area?
- Answer: Not currently.
- Evidence:
  - Limited top-level sections under `docs/Docusaurus/docs/`.
  - Sidebar is fully autogenerated from folder structure.
- Triangulation: filesystem + sidebar config.

## (B) Questions for user (explicit options)

### Q1 — API surface style for host builders
- A. Fluent interface only (`services.AddClientBuilder().AddDomainX().Build()`).
- B. Lambda-config over extension entrypoint (`services.AddClient(builder => builder.AddDomainX())`).
- C. Both (fluent object + lambda wrapper).
- X. I don’t care — pick repo-consistent default.
- Recommended default if X: C (matches existing `AddInletBlazorSignalR(Action<Builder>)` while enabling direct fluent usage).
- Impact:
  - A: maximal explicitness, larger API surface.
  - B: smallest surface, easiest migration from current extension style.
  - C: best DX flexibility, moderate API complexity.

### Q2 — Builder contracts location (interfaces)
- A. New cross-cutting abstractions package(s) for all builder interfaces.
- B. Interfaces split by area (`Inlet.Client.Abstractions`, `Inlet.Gateway.Abstractions`, `DomainModeling.Abstractions`, `Reservoir.Abstractions`).
- C. Keep concrete builders only (no public interfaces except where strictly needed).
- X. I don’t care — pick repo-consistent default.
- Recommended default if X: B (aligns with existing area-based abstractions and dependency boundaries).
- Impact:
  - A: unified discoverability but may over-centralize dependencies.
  - B: strongest layering alignment and package granularity.
  - C: faster initial rollout but weaker contract stability for consumers/mocking.

### Q3 — Required call-order strictness
- A. Compile-time guide only (method chaining with sensible defaults; runtime validation at `Build()`).
- B. Strong runtime guards (throw on missing required steps).
- C. Type-state builders enforcing order at compile time (more generic complexity).
- X. I don’t care — pick repo-consistent default.
- Recommended default if X: A now, with targeted runtime guards for critical prereqs.
- Impact:
  - A: best ergonomics, low complexity.
  - B: safer at runtime, still simple API.
  - C: strongest correctness, highest complexity and cognitive load.

### Q4 — Scope of v1 builder rollout
- A. Host builders only (`IClientBuilder`, `IGatewayBuilder`, `IRuntimeBuilder`) + adapter hooks for feature builders.
- B. Host + all requested feature builders in one phase.
- C. Feature builders first, host builders later.
- X. I don’t care — pick repo-consistent default.
- Recommended default if X: B for design completeness, staged implementation behind one compatibility plan.
- Impact:
  - A: lower risk, less immediate DX payoff.
  - B: cohesive developer story, larger initial change.
  - C: less intuitive onboarding narrative for new users.

### Q5 — Prototype-and-delete strategy
- A. Build throwaway prototype/tests in temporary branch/folder and delete before docs PR.
- B. Keep prototype tests in repo as long-term executable spec.
- C. Hybrid: keep a minimal contract test suite, remove exploratory tests.
- X. I don’t care — pick repo-consistent default.
- Recommended default if X: C (preserves proof-of-concept value while avoiding test clutter).
- Impact:
  - A: clean history but loses executable proof.
  - B: strongest regression safety, but expands maintenance surface.
  - C: balanced evidence + maintainability.

## CoV
- Claim: These are the highest-leverage unresolved decisions for architecture and docs path.
  - Evidence: user request scope + repo findings `F1..F8`.
  - Triangulation: direct user ask + current code/doc topology.
  - Confidence: High.
  - Impact: Answers will drive `03-decisions.md` and `04-draft-plan.md`.